First of  all create a file named avl.graphviz.txt in any location and copy the path
of the file and paste it in fp.open() command of the program.

Make sure graphviz is installed on your system.

just do build and run and enter your choice of which operation you need to perform.

